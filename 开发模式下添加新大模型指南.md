# 开发模式添加新模型指南

本文档旨在指导开发者或高级用户如何在《智者计划》项目中添加新的 AI 模型或接入新的 AI 服务商。

随着系统功能迭代，我们现在提供了**两种完全不同的添加方式**：

1. **界面动态添加（强烈推荐）**：无需修改任何代码，直接在应用程序的 UI 中配置，即刻生效。
2. **代码级硬编码接入（开发者进阶）**：通过修改底层注册表和编写代码，将服务商永久固化到系统默认列表中。

本项目底层采用了 **注册表模式 (Registry Pattern)** 和 **工厂模式 (Factory Pattern)** 来管理 AI 服务，使得系统扩展异常灵活。

---

## 1. 界面动态添加（强烈推荐，无需改代码）

目前系统已完全支持在运行时动态注册兼容 OpenAI 标准的服务商及模型（包括本地部署的 Ollama、vLLM 等），强烈推荐绝大多数情况下使用此方法，完全告别改代码和重启的烦恼。

### 场景 A：添加全新的自定义服务商（如 Ollama, Groq, 阶跃星辰等）

1. 在应用程序中打开 **"设置" -> "API 密钥配置"**。
2. 在左侧服务商列表的最下方，点击 **"+ 新增服务商"**。
3. 在面版中填写极其精简的信息：
   - **名称 (显示用)**: 例如 `Ollama 本地`
   - **API Base URL**: 例如 `http://localhost:11434/v1`（注意：底层的智能引擎会自动处理末尾斜杠和多余的路径映射）。
   - **默认模型 ID**: 例如 `llama3`
   - **区域归属**: 选择合适的区域以供 UI 分组。
4. 点击 **"确认添加"**。后台会自动生成无碰撞的唯一内部 ID，新服务商瞬间即可投入使用。

### 场景 B：为任意服务商（内置或自定义）添加或修改模型

1. 选中任意一个服务商核心。
2. 在右侧的“模型配置”面板下，点击 **"为此服务商添加模型"**（如果是自定义模型，也可以直接点击列表旁边的“编辑”按钮）。
3. 在弹出的表单中填写核心参数：
   - **模型真实 ID**: 例如 `deepseek-v4-flash`
   - **模型友好显示名称**: 例如 `DeepSeek V4 Flash`
4. （可选）展开**高级设置**以深度定制：
   - 勾选模型是否支持 **Tool Calling (工具调用)** 或 **Vision (视觉多模态)**。
   - 配置 **Max Input Tokens** 与 **Max Output Tokens** 以精确覆盖模型的上下文限制。
5. 点击 **"确认添加"**。该模型立刻会出现在下拉列表中，选中即刻生效！
   - _智能回退机制：当您添加第一个可用模型时，如果它与原本记录的默认模型不同，系统会自动容错并切换至您的真实模型。_

---

## 2. 代码级硬编码接入（开发者进阶）

如果您是开发者，并且需要将某个服务商永久打包固化进应用的**默认支持列表**中，或者您需要接入一个**完全非标准**（不支持 OpenAI 格式）的特殊 API，则需要修改源码。

### 2.1 核心架构与文件结构

在动手之前，请先了解 `electron/providers/` 目录下的核心文件及其职责：

| 文件         | 路径                                                | 职责                                                                                                       |
| :----------- | :-------------------------------------------------- | :--------------------------------------------------------------------------------------------------------- |
| **注册表**   | `electron/providers/provider-registry.cjs`          | **核心配置中心**。定义所有默认支持的厂商、API 地址、模型列表、速率限制等静态数据。90% 的修改都在这里完成。 |
| **通用驱动** | `electron/providers/openai-compatible-provider.cjs` | **通用适配器**。处理所有兼容 OpenAI 接口格式的厂商（如 DeepSeek、智谱、Kimi、Groq 等）。                   |
| **工厂**     | `electron/providers/provider-factory.cjs`           | **实例化工厂**。根据配置中的 `type` 字段，决定实例化哪个驱动类。                                           |
| **AI 服务**  | `electron/ai-service.cjs`                           | **业务逻辑层**。处理 API 调用、错误重试、速率限制管理以及特定厂商的错误码映射。                            |

### 2.2 给现有的内置厂商硬编码新模型

**场景**：例如 SiliconFlow 官方上架了新的 `DeepSeek-V4`。

1.  打开 `electron/providers/provider-registry.cjs`。
2.  找到对应厂商的配置块（例如 `siliconflow`）。
3.  在 `models` 数组中追加新的模型对象：

```javascript
{
  // 【必填】API调用时的真实模型ID
  id: 'deepseek-ai/DeepSeek-V4',
  name: 'DeepSeek-V4',
  description: '最新旗舰模型',
  // 【可选】速率限制配置
  rateLimits: { rpm: 1000, tpm: 100000 }
}
```

4.  保存并**重启应用**。

### 2.3 硬编码新的 OpenAI 兼容厂商

1.  打开 `electron/providers/provider-registry.cjs`。
2.  在 `PROVIDER_REGISTRY` 对象中添加一个新的厂商配置对象：

```javascript
  deepseek: {
    id: 'deepseek',
    name: 'Deep Seek',
    type: 'openai-compatible',
    baseUrl: 'https://api.deepseek.com',
    defaultModel: 'deepseek-v4-pro',
    models: [
      { id: 'deepseek-v4-pro', name: 'deepseek-v4-pro', description: '深度求索旗舰级 1.6T 深度思考模型 (DeepSeek-V4-Pro)' },
      { id: 'deepseek-v4-flash', name: 'deepseek-v4-flash', description: '深度求索高性价比极速模型 (DeepSeek-V4-Flash)' },
    ],
    region: 'china',
    requiresProxy: false,
    note: '前往 DeepSeek 开放平台获取 API Key',
  },
```

3.  **无需修改其他文件**，重启应用前端会自动读取并渲染。

### 2.4 添加非标准 API 厂商（进阶）

**场景**：接入像旧版 Claude 那样完全不支持 OpenAI 格式的 API。

1.  **创建驱动文件**：在 `electron/providers/` 下新建 `my-custom-provider.cjs`，继承 `BaseProvider` 并实现 `complete` 方法。
2.  **注册配置**：在 `provider-registry.cjs` 中将 `type` 设置为 `my-custom`。
3.  **注册工厂**：打开 `electron/providers/provider-factory.cjs`，引入新类，并在 `createProvider` 函数的 `switch` 中添加对应的实例化分支。

---

## 3. 进阶：自定义错误处理

为了提供更好的用户体验，可以在 `electron/ai-service.cjs` 中添加针对该厂商的精准错误码映射。

1. 找到 `testConnection` 方法中的错误处理逻辑（约第 260 行）。
2. 在 `has` 辅助函数中，添加该厂商特有的错误关键词：

```javascript
// 示例：添加智谱 AI 的特定错误码 1001
else if (has('1001', 'signature')) {
  errorMessage = '智谱AI：JWT 签名校验失败，请检查 API Key 是否正确';
}
```

---

## 4. 验证与调试

- **动态添加**：修改立即生效，所见即所得。
- **修改源码**：Electron 主进程代码修改后必须重启才能生效（终端运行 `npm run dev`）。
- **测试连通性**：在设置面板中填入 API Key 后，点击 **"测试连接"**。若出现 "✓ 连接成功" 及极速响应时间，即代表模型/服务商配置完美无缺！

**祝您在智者计划的世界中开发愉快！**
